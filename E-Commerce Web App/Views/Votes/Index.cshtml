@model IEnumerable<E_Commerce_Web_App.Models.Vote>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Option)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Option.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Vote_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Vote_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Vote_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
