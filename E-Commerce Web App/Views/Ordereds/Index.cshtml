@model IEnumerable<E_Commerce_Web_App.Models.Ordered>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductVariant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductVariant.ProductVariant_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ordered_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ordered_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ordered_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
